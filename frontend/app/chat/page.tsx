import { NewConversation } from "@/components/NewConversation";

export const metadata = { title: "New conversation · LLM Gateway" };

export default function NewChatPage() {
  return <NewConversation />;
}
